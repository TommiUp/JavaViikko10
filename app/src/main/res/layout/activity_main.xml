<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/listUsersButton"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginStart="108dp"
        android:layout_marginTop="49dp"
        android:layout_marginEnd="108dp"
        android:text="Listaa käyttäjät"
        android:textAlignment="center"
        android:onClick="switchToUserList"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/addUsersButton" />

    <Button
        android:id="@+id/addUsersButton"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginStart="108dp"
        android:layout_marginTop="117dp"
        android:layout_marginEnd="108dp"
        android:text="Lisää käyttäjiä"
        android:textAlignment="center"
        android:onClick="switchToAddUser"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/saveButton"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_marginTop="49dp"
        android:onClick="saveUsers"
        android:text="Tallenna käyttäjät"
        app:layout_constraintEnd_toEndOf="@+id/listUsersButton"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/listUsersButton"
        app:layout_constraintTop_toBottomOf="@+id/listUsersButton" />

    <Button
        android:id="@+id/loadButton"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginTop="49dp"
        android:onClick="loadUsers"
        android:text="Lataa käyttäjät"
        app:layout_constraintEnd_toEndOf="@+id/saveButton"
        app:layout_constraintStart_toStartOf="@+id/saveButton"
        app:layout_constraintTop_toBottomOf="@+id/saveButton" />

</androidx.constraintlayout.widget.ConstraintLayout>